---
api: "GET /api/v1.0/{userCode}/balance"
title: "Balance"
---

Retorna o saldo de créditos da empresa autenticada.

## Endpoint

<Request
  method="GET"
  url="https://api.thatsme.com.br/api/v1.0/{userCode}/balance"
/>

> 🔐 **Autenticação**
>
> Este endpoint requer autenticação via **Bearer Token**.
> Informe o token no campo exibido acima.  
> O header `Authorization: Bearer <token>` será aplicado automaticamente.

## Response Fields

<ResponseField name="balance" type="number">
  Saldo atual de créditos da empresa em reais (R$).
</ResponseField>

<ResponseField name="profile_id" type="string">
  ID único do perfil da empresa.
</ResponseField>

<ResponseField name="company_name" type="string">
  Nome da empresa.
</ResponseField>

## Response Example

<Response status="200" type="object">

```json
{
  "balance": 150.5,
  "profile_id": "profile_123",
  "company_name": "Nome da Empresa"
}
