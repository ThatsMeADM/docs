---
api: "GET /api/v1.0/{userCode}/badges"
title: "List Badges"
---

Lista badges (modelos de badges).

<Request method="GET" url="/api/v1.0/{userCode}/badges" auth="bearer" />

<ParamField path="userCode" type="string" required placeholder="ABC123XYZ">
  Código do usuário usado no path da rota.
</ParamField>

<ParamField path="query.issuerId" type="string">
  ID do emissor (opcional)
</ParamField>

<ParamField path="query.limit" type="number">
  Limite de resultados
</ParamField>

<ParamField path="query.offset" type="number">
  Offset para paginação
</ParamField>

<Expandable title="properties">
  <ResponseField name="data" type="array">
    Lista de badges
  </ResponseField>
  
  <ResponseField name="total" type="number">
    Total de resultados encontrados
  </ResponseField>
  
  <ResponseField name="limit" type="number">
    Limite de resultados por página
  </ResponseField>
  
  <ResponseField name="offset" type="number">
    Offset usado na paginação
  </ResponseField>
</Expandable>

<Response status="200" type="object">

```json
{
  "data": [
    {
      "badge_id": "badge_123",
      "name": "Certificado de Participação",
      "description": "Badge para participantes do evento",
      "created_at": "2024-01-10T09:00:00Z",
      "assets": {
        "image_url": "https://example.com/badge.png"
      },
      "user": {
        "profile": {
          "profile_id": "profile_456",
          "name": "Organizador",
          "company_name": "Empresa Organizadora"
        }
      }
    }
  ],
  "total": 15,
  "limit": 50,
  "offset": 0
}
```

</Response>
